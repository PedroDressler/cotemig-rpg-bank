import styled from "styled-components";

export const Title = styled.span`
  display: block;
  font-size: 3rem;
  font-weight: bold;
  width: auto;
  height: auto;
`;

export const Text = styled.span`
  display: block;
  font-size: 1rem;
  font-weight: 200;
  width: auto;
  height: auto;
`;

export default {
  Title,
  Text
}